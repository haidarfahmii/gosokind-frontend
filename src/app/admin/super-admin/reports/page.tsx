// Super Admin reports page re-uses the Outlet Admin implementation.
// Backend enforces outlet scoping per role; Super Admin sees the outlet filter dropdown.
export { default } from "@/app/admin/outlet-admin/reports/page";
